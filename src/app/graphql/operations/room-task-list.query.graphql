query RoomTaskList($id: ID!, $offset: Int) {
  room(id: $id) {
    id
    tasks(limit: 20, offset: $offset, order: { id: DESC }, isOwn: true) {
      total
      results {
        ...Task
      }
    }
  }
}
