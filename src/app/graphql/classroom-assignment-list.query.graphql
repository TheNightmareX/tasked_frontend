query ClassroomAssignmentList(
  $id: ID!
  $limit: Int
  $offset: Int
  $isCompleted: Boolean
  $isPublic: Boolean
  $isOwn: Boolean
) {
  classroom(id: $id) {
    id
    assignments(
      limit: $limit
      offset: $offset
      isCompleted: $isCompleted
      isPublic: $isPublic
      isOwn: $isOwn
    ) {
      total
      results {
        id
        recipient {
          id
        }
        task {
          id
          title
          description
          creator {
            id
            username
            nickname
          }
        }
        isPublic
        isCompleted
        isImportant
        createdAt
      }
    }
  }
}
