query ClassroomMembershipList($id: ID!) {
  classroom(id: $id) {
    id
    creator {
      id
      username
    }
    memberships {
      total
      results {
        owner {
          id
          username
          nickname
          gender
        }
        displayName
        role
      }
    }
  }
}
