query ClassroomMembershipList($id: ID!) {
  classroom(id: $id) {
    id
    memberships {
      total
      results {
        id
        owner {
          id
          username
          nickname
          gender
        }
        displayName
        role
      }
    }
  }
}
