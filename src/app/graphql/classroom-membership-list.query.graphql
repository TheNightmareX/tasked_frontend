query ClassroomMembershipList($id: ID!) {
  classroom(id: $id) {
    id
    creator {
      id
    }
    memberships {
      total
      results {
        owner {
          id
          username
          nickname
          gender
        }
        displayName
        role
      }
    }
  }
}
