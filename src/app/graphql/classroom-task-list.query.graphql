query ClassroomTaskList($id: ID!, $limit: Int, $offset: Int) {
  classroom(id: $id) {
    id
    tasks(limit: $limit, offset: $offset) {
      total
      results {
        ...Task
      }
    }
  }
}
